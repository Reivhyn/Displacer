import { createContext } from 'react'

export const contextTierTiles = createContext('')

export const contextPlacedTiles = createContext('')

export const contextTierLabels = createContext('')

export const contextTierColors = createContext('')

export const contextSelectionBoard = createContext('')

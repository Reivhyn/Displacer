import React, { useContext } from 'react'

// * CONTEXT

const TierTile = (image) => {
  return (
    <>
      tierTile
      <img src={image} alt="" style={{ width: '300px', height: '423px' }} />
    </>
  )
}

export default TierTile
